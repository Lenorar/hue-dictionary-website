    <h4>
        <%= word.title %>
    </h4>
    <h4>
        <%= word.definition %>
    </h4>


<a href="<%= `/word/${word.id}/edit`%>">Edit movie details</a> |

<form action="<%= `/word/${word.id}?_method=DELETE` %>" method="POST">
  <input type="submit" value="Delete" />
